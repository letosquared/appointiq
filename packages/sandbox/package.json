{
  "name": "@appointiq/sandbox",
  "version": "0.1.0",
  "description": "A GoHighLevel-compatible API: the same routes, payload shapes and error codes as the real platform, running on a pluggable store. Powers the $0 demo.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@appointiq/ghl": "*",
    "@upstash/redis": "^1.34.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3",
    "vitest": "^2.1.9"
  }
}
